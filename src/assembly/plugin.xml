<assembly xmlns="http://maven.apache.org/ASSEMBLY/2.1.1"
          xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
          xsi:schemaLocation="http://maven.apache.org/ASSEMBLY/2.1.1 http://maven.apache.org/xsd/assembly-2.1.1.xsd">
    <id>plugin</id>
    <formats>
        <format>zip</format>
    </formats>
    <includeBaseDirectory>false</includeBaseDirectory>
    <files>
        <file>
            <!-- The jar built by maven-jar-plugin (finalName = CallerView). -->
            <source>${project.build.directory}/${project.build.finalName}.jar</source>
            <!-- The zip MUST contain a single top-level folder named after the plugin:
                 CallerView/lib/CallerView.jar
                 IDEA (PluginDescriptorLoader.loadDescriptorFromArtifact) takes the first
                 top-level entry of the zip as the plugin dir. Without the wrapping
                 CallerView/ folder it picks "lib" and fails with
                 "Fail to load plugin descriptor from file". -->
            <outputDirectory>CallerView/lib</outputDirectory>
            <destName>CallerView.jar</destName>
        </file>
    </files>
</assembly>
